# EXP-001 core stack. Keep these constraints on every dependency install.
torch==2.0.1+cu118
torchvision==0.15.2+cu118
ultralytics==8.0.20

# NumPy 2.x is ABI-incompatible with extensions built against NumPy 1.x.
numpy==1.26.4

# pkg_resources was removed from setuptools distributions starting at 82.0.0.
setuptools>=65,<82

# OpenCV 4.12+ requires NumPy 2.x on Python 3.9 and newer.
opencv-python<4.12
opencv-python-headless<4.12
