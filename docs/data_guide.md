# Data Guide

TODO: Document the selected datasets and download sources, local directory layout, YOLO label format, Fire / Smoke class definition, negative data policy, train/validation/test split process, and dataset version management.

The test set must remain separate from model training and tuning. Unverified dataset URLs and class mappings must not be added here.